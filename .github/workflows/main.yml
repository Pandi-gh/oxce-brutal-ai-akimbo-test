name: Build Custom OXCE (Windows)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    # 1. Включаем кэш для скачанных пакетов MSYS2
    - name: Setup MSYS2
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        cache: true
        install: >-
          base-devel
          mingw-w64-x86_64-toolchain
          mingw-w64-x86_64-cmake
          mingw-w64-x86_64-yaml-cpp
          mingw-w64-x86_64-glew
          mingw-w64-x86_64-SDL
          mingw-w64-x86_64-SDL_mixer
          mingw-w64-x86_64-SDL_image
          mingw-w64-x86_64-SDL_gfx
          mingw-w64-x86_64-libmad
          mingw-w64-x86_64-libvorbis
          mingw-w64-x86_64-flac
          mingw-w64-x86_64-libmodplug
          mingw-w64-x86_64-libpng
          mingw-w64-x86_64-libjpeg-turbo
          mingw-w64-x86_64-libtiff
          mingw-w64-x86_64-libwebp
          mingw-w64-x86_64-zstd
          mingw-w64-x86_64-xz
          mingw-w64-x86_64-jbigkit
          mingw-w64-x86_64-libdeflate
          mingw-w64-x86_64-lerc
          mingw-w64-x86_64-ccache

    # 2. Кэш компилятора
    - name: Setup ccache cache
      uses: actions/cache@v4
      with:
        path: .ccache
        key: ccache-windows-${{ github.sha }}
        restore-keys: |
          ccache-windows-

    - name: Configure ccache
      run: |
        ccache --set-config=cache_dir="$GITHUB_WORKSPACE/.ccache"
        ccache --max-size=1G
        ccache -z

    # 3. ВАЖНО: создаём ПУСТЫЕ data-папки в исходниках ДО Configure/Build, потому
    # что OpenXcom CMake POST_BUILD копирует bin/{TFTD,UFO,common,standard} в
    # build/bin/, и copy_directory падает если исходной папки нет.
    # TFTD и UFO в git не индексируются (пустые), их надо создать руками.
    - name: Create missing data directories
      run: |
        mkdir -p bin/TFTD
        mkdir -p bin/UFO
        mkdir -p bin/common
        mkdir -p bin/standard

    - name: Configure CMake
      run: >
        cmake -G "MinGW Makefiles" -B build
        -DCMAKE_BUILD_TYPE=Release
        -DCMAKE_C_COMPILER_LAUNCHER=ccache
        -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
        -DCMAKE_FIND_LIBRARY_SUFFIXES=".dll.a;.a"
        -DCMAKE_CXX_STANDARD_LIBRARIES="-Wl,--start-group -lSDL_mixer -lSDL_image -lSDL_gfx -lSDL -l:libFLAC.dll.a -lvorbisfile -lvorbis -logg -lmodplug -lmad -ltiff -lwebp -lsharpyuv -lLerc -ljbig -ldeflate -ljpeg -lpng16 -lzstd -llzma -lz -lbz2 -ldxguid -ldinput8 -ldinput -lwinmm -lole32 -loleaut32 -lsetupapi -luuid -limm32 -lversion -lws2_32 -Wl,--end-group"

    - name: Build
      run: cmake --build build -j$(nproc)

    - name: Check ccache statistics
      run: ccache -s

    - name: Bundle required DLLs
      run: |
        ldd build/bin/openxcom.exe | grep /mingw64/bin | awk '{print $3}' | xargs -I {} cp -n {} build/bin/ || true
        cp -n /mingw64/bin/libmad-0.dll build/bin/ || true
        cp -n /mingw64/bin/libmpg123-0.dll build/bin/ || true
        cp -n /mingw64/bin/libvorbisfile-3.dll build/bin/ || true
        cp -n /mingw64/bin/libvorbis-0.dll build/bin/ || true
        cp -n /mingw64/bin/libogg-0.dll build/bin/ || true
        cp -n /mingw64/bin/libFLAC-8.dll build/bin/ || true
        cp -n /mingw64/bin/libmodplug-1.dll build/bin/ || true
        cp -n /mingw64/bin/libopusfile-0.dll build/bin/ || true
        cp -n /mingw64/bin/libopus-0.dll build/bin/ || true
        cp -n /mingw64/bin/libgcc_s_seh-1.dll build/bin/ || true
        cp -n /mingw64/bin/libstdc++-6.dll build/bin/ || true
        cp -n /mingw64/bin/libwinpthread-1.dll build/bin/ || true

    # 4. pWWWa/test: гарантируем что в финальном артефакте лежат resource folders.
    # CMake POST_BUILD уже копирует их в build/bin/, но если что-то пошло не так
    # (например пустая bin/common из шага 3) — дублируем здесь. На пустую TFTD/UFO
    # это не повлияет, а common/ и standard/ будут заведомо актуальные из репо.
    - name: Mirror resources into artifact folder
      run: |
        mkdir -p build/bin/common
        mkdir -p build/bin/standard
        cp -r bin/common/* build/bin/common/ 2>/dev/null || true
        cp -r bin/standard/* build/bin/standard/ 2>/dev/null || true
        # Контрольная проверка: dont-touch.me должен быть в common/
        if [ ! -f build/bin/common/dont-touch.me ]; then
          echo "WARNING: build/bin/common/dont-touch.me is missing, game will refuse to start!"
          ls -la build/bin/common/ | head -20
        else
          echo "OK: dont-touch.me present:"
          cat build/bin/common/dont-touch.me
        fi

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: OpenXcom-windows-build
        path: build/bin/
